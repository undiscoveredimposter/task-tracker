---
name: Work item
about: A backlog item for one of the agents to pick up
title: 'area: short description in the imperative'
labels: 'status:ready'
assignees: ''
---

<!--
LABELS. Every issue carries exactly one `area:` label and one `status:` label.

`status:ready` has been applied for you. Add the area yourself — `area:frontend`,
`area:backend` or `area:infra`. Without one no agent will ever find this issue.

If it cannot start yet, open the body with a bold "**Blocked by #N** — why", and
use `status:blocked` instead.

Full lifecycle: docs/AGENTS.md#labels
-->

<!--
What is wrong, missing, or worth doing — and why it matters. One or two paragraphs.

Name the files if you already know them; it saves the agent a search. Include the
evidence if there is any: the error text, the measured value, the failing command.
-->

## Scope

<!--
The bullets that make up the change, and anything deliberately outside it. "Stay in
scope" is a working agreement, and an unbounded issue produces an unbounded diff.

Drop this section for a fix small enough to be self-evident.
-->

-

## Done when

<!--
Observable outcomes, not tasks. "Reordering persists across a reload", not "add a move
endpoint". The reviewer checks the pull request against this list, so anything not
written here will not be enforced — and anything already true of `main` should not be
here at all.
-->

-
